# Foundry
contracts/cache/
contracts/out/
contracts/broadcast/
contracts/lib/

# Dependencies
node_modules/

# Build artifacts
dist/
web/dist/
*.tsbuildinfo

# Local environment / secrets
.env
.env.local

# Relayer local state (SQLite db of processed events)
*.db
*.db-journal
*.db-shm
*.db-wal

# Yarn (node-modules linker; no zero-installs)
**/.yarn/install-state.gz
**/.yarn/cache/

# Deployed addresses (generated by deploy scripts; may contain local-only addrs)
deployed-addresses.json

# E2E local logs
**/.e2e-relayer.log
